<div class="card my-2" data-commentid = "{{id}}">
  <div class="card-body">
    <p class="card-title">Comment by {{user_id}} on {{createdAt}}</p>
    <p class="card-text">{{content}}</p>
  </div>
</div>